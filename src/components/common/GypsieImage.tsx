import { View } from 'react-native';

const GypsieImage = () => {
  // To replace normal Image with a check on whether photo is loaded, if not show GypsieSkeleton

  return <View>{/* <Image />  */}</View>;
};

export default GypsieImage;
