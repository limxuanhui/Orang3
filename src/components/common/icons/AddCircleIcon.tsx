import Icon from "react-native-vector-icons/FontAwesome";
import type { IconProps } from "./types/types";

const AddCircleIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="plus-circle" />;
};

export default AddCircleIcon;
