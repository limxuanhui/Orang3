import Icon from 'react-native-vector-icons/Entypo';
import type { IconProps } from './types/types';

const ChevronDownIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="chevron-down" />;
};

export default ChevronDownIcon;
