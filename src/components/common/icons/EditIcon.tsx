import Icon from 'react-native-vector-icons/AntDesign';
import type { IconProps } from './types/types';

const EditIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="edit" />;
};

export default EditIcon;
