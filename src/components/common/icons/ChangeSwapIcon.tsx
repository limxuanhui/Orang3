import Icon from "react-native-vector-icons/Entypo";
import type { IconProps } from "./types/types";

const ChangeSwapIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="cycle" />;
};

export default ChangeSwapIcon;
