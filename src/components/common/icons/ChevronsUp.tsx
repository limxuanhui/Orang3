import Icon from "react-native-vector-icons/Feather";
import type { IconProps } from "./types/types";

const ChevronsUpIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="chevrons-up" />;
};

export default ChevronsUpIcon;
