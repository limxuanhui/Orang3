import Icon from 'react-native-vector-icons/Ionicons';
import type { IconProps } from './types/types';

const GridIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="grid" />;
};

export default GridIcon;
