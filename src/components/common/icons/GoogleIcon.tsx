import Icon from "react-native-vector-icons/AntDesign";
import type { IconProps } from "./types/types";

const GoogleIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="google" />;
};

export default GoogleIcon;
