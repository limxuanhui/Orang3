import Icon from 'react-native-vector-icons/Entypo';
import type { IconProps } from './types/types';

const ChevronRightIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="chevron-right" />;
};

export default ChevronRightIcon;
