import Icon from 'react-native-vector-icons/FontAwesome6';
import type { IconProps } from './types/types';

const DeleteLeftIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="delete-left" />;
};

export default DeleteLeftIcon;
