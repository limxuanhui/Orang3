import Icon from 'react-native-vector-icons/Entypo';
import type { IconProps } from './types/types';

const ChevronUpIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="chevron-up" />;
};

export default ChevronUpIcon;
