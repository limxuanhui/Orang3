import Icon from "react-native-vector-icons/FontAwesome6";
import { IconProps } from "./types/types";

const BookOpenIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="book-open" />;
};

export default BookOpenIcon;
