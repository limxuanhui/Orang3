import Icon from 'react-native-vector-icons/Ionicons';
import type { IconProps } from './types/types';

const GridOutlineIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="grid-outline" />;
};

export default GridOutlineIcon;
