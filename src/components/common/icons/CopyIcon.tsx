import Icon from 'react-native-vector-icons/Octicons';
import type { IconProps } from './types/types';

const CloneIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="copy" />;
};

export default CloneIcon;
