import Icon from "react-native-vector-icons/MaterialIcons";
import type { IconProps } from "./types/types";

const AddMoreIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="add-to-photos" />;
};

export default AddMoreIcon;
