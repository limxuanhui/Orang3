import Icon from 'react-native-vector-icons/Entypo';
import type { IconProps } from './types/types';

const FolderImagesIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="folder-images" />;
};

export default FolderImagesIcon;
