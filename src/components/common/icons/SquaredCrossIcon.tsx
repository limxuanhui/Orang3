import Icon from "react-native-vector-icons/Entypo";
import type { IconProps } from "./types/types";

const SquaredCrossIcon = ({ style }: IconProps) => {
  return <Icon style={style} name="squared-cross" />;
};

export default SquaredCrossIcon;
